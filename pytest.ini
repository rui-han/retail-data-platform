[pytest]
testpaths = tests
pythonpath = .
# Show local variables on failure for easier debugging.
addopts = -v --tb=short
# Suppress verbose Java/Py4J log spam from PySpark.
log_cli = false
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning